{
  "name": "moneytowords-fsharp",
  "version": "0.1.0",
  "private": true,
  "description": "F# library for converting numbers to words",
  "scripts": {
    "docs": "dotnet fsi .github/scripts/generate-contributing.fsx",
    "format": "find .github/scripts -name \"*.fsx\" -exec fantomas {} \\;",
    "format:check": "find .github/scripts -name \"*.fsx\" -exec fantomas --check {} \\; || exit 1",
    "type-check": "find .github/scripts -name \"*.fsx\" -exec dotnet fsi --check {} \\; || exit 1",
    "verify": "npm run docs && git diff --exit-code CONTRIBUTING.md || (echo '❌ CONTRIBUTING.md out of date!' && exit 1) && npm run format:check && npm run type-check",
    "ci-check": "npm run verify",
    "labels:sync": "dotnet fsi .github/scripts/sync-labels.fsx",
    "labels:sync:dry-run": "dotnet fsi .github/scripts/sync-labels.fsx -- --dry-run --quiet",
    "setup": "npm install && npm run labels:sync"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "npm@11.8.0"
}
