namespace MoneyToWords.Tests

open Swensen.Unquote
open Xunit
open MoneyToWords.Domain
open MoneyToWords.Presentation

module KopeckSpellingTests =

    /// Вспомогательная функция: безопасно создаёт Money и проверяет результат
    let expectKopeckSpelling rubles kopecks expected =
        match Money.TryCreate(rubles, kopecks) with
        | Ok money ->
            let actual = MoneyToWords.toWords money
            test <@ actual = expected @>
        | Error _ ->
            test <@ false @>  // Это не должно произойти при валидных входах

    /// === Проверка копеек при 0 рублях ===

    [<Fact>]
    let ``0.01 → ноль рублей и одна копейка`` () =
        expectKopeckSpelling 0L 1 "ноль рублей и одна копейка"

    [<Fact>]
    let ``0.02 → ноль рублей и две копейки`` () =
        expectKopeckSpelling 0L 2 "ноль рублей и две копейки"

    [<Fact>]
    let ``0.03 → ноль рублей и три копейки`` () =
        expectKopeckSpelling 0L 3 "ноль рублей и три копейки"

    [<Fact>]
    let ``0.04 → ноль рублей и четыре копейки`` () =
        expectKopeckSpelling 0L 4 "ноль рублей и четыре копейки"

    [<Fact>]
    let ``0.05 → ноль рублей и пять копеек`` () =
        expectKopeckSpelling 0L 5 "ноль рублей и пять копеек"

    [<Fact>]
    let ``0.06 → ноль рублей и шесть копеек`` () =
        expectKopeckSpelling 0L 6 "ноль рублей и шесть копеек"

    [<Fact>]
    let ``0.09 → ноль рублей и девять копеек`` () =
        expectKopeckSpelling 0L 9 "ноль рублей и девять копеек"

    [<Fact>]
    let ``0.10 → ноль рублей и десять копеек`` () =
        expectKopeckSpelling 0L 10 "ноль рублей и десять копеек"

    [<Fact>]
    let ``0.11 → ноль рублей и одиннадцать копеек`` () =
        expectKopeckSpelling 0L 11 "ноль рублей и одиннадцать копеек"

    [<Fact>]
    let ``0.12 → ноль рублей и двенадцать копеек`` () =
        expectKopeckSpelling 0L 12 "ноль рублей и двенадцать копеек"

    [<Fact>]
    let ``0.13 → ноль рублей и тринадцать копеек`` () =
        expectKopeckSpelling 0L 13 "ноль рублей и тринадцать копеек"

    [<Fact>]
    let ``0.14 → ноль рублей и четырнадцать копеек`` () =
        expectKopeckSpelling 0L 14 "ноль рублей и четырнадцать копеек"

    [<Fact>]
    let ``0.15 → ноль рублей и пятнадцать копеек`` () =
        expectKopeckSpelling 0L 15 "ноль рублей и пятнадцать копеек"

    [<Fact>]
    let ``0.20 → ноль рублей и двадцать копеек`` () =
        expectKopeckSpelling 0L 20 "ноль рублей и двадцать копеек"

    [<Fact>]
    let ``0.21 → ноль рублей и двадцать одна копейка`` () =
        expectKopeckSpelling 0L 21 "ноль рублей и двадцать одна копейка"

    [<Fact>]
    let ``0.22 → ноль рублей и двадцать две копейки`` () =
        expectKopeckSpelling 0L 22 "ноль рублей и двадцать две копейки"

    [<Fact>]
    let ``0.23 → ноль рублей и двадцать три копейки`` () =
        expectKopeckSpelling 0L 23 "ноль рублей и двадцать три копейки"

    [<Fact>]
    let ``0.24 → ноль рублей и двадцать четыре копейки`` () =
        expectKopeckSpelling 0L 24 "ноль рублей и двадцать четыре копейки"

    [<Fact>]
    let ``0.25 → ноль рублей и двадцать пять копеек`` () =
        expectKopeckSpelling 0L 25 "ноль рублей и двадцать пять копеек"

    [<Fact>]
    let ``0.26 → ноль рублей и двадцать шесть копеек`` () =
        expectKopeckSpelling 0L 26 "ноль рублей и двадцать шесть копеек"

    [<Fact>]
    let ``0.29 → ноль рублей и двадцать девять копеек`` () =
        expectKopeckSpelling 0L 29 "ноль рублей и двадцать девять копеек"

    [<Fact>]
    let ``0.30 → ноль рублей и тридцать копеек`` () =
        expectKopeckSpelling 0L 30 "ноль рублей и тридцать копеек"

    [<Fact>]
    let ``0.40 → ноль рублей и сорок копеек`` () =
        expectKopeckSpelling 0L 40 "ноль рублей и сорок копеек"

    [<Fact>]
    let ``0.50 → ноль рублей и пятьдесят копеек`` () =
        expectKopeckSpelling 0L 50 "ноль рублей и пятьдесят копеек"

    [<Fact>]
    let ``0.60 → ноль рублей и шестьдесят копеек`` () =
        expectKopeckSpelling 0L 60 "ноль рублей и шестьдесят копеек"

    [<Fact>]
    let ``0.70 → ноль рублей и семьдесят копеек`` () =
        expectKopeckSpelling 0L 70 "ноль рублей и семьдесят копеек"

    [<Fact>]
    let ``0.80 → ноль рублей и восемьдесят копеек`` () =
        expectKopeckSpelling 0L 80 "ноль рублей и восемьдесят копеек"

    [<Fact>]
    let ``0.90 → ноль рублей и девяносто копеек`` () =
        expectKopeckSpelling 0L 90 "ноль рублей и девяносто копеек"

    [<Fact>]
    let ``0.91 → ноль рублей и девяносто одна копейка`` () =
        expectKopeckSpelling 0L 91 "ноль рублей и девяносто одна копейка"

    [<Fact>]
    let ``0.92 → ноль рублей и девяносто две копейки`` () =
        expectKopeckSpelling 0L 92 "ноль рублей и девяносто две копейки"

    [<Fact>]
    let ``0.95 → ноль рублей и девяносто пять копеек`` () =
        expectKopeckSpelling 0L 95 "ноль рублей и девяносто пять копеек"

    [<Fact>]
    let ``0.99 → ноль рублей и девяносто девять копеек`` () =
        expectKopeckSpelling 0L 99 "ноль рублей и девяносто девять копеек"

    /// === Проверка копеек при 1 рубле (чтобы убедиться в корректности составного вывода) ===

    [<Fact>]
    let ``1.01 → один рубль и одна копейка`` () =
        expectKopeckSpelling 1L 1 "один рубль и одна копейка"

    [<Fact>]
    let ``1.02 → один рубль и две копейки`` () =
        expectKopeckSpelling 1L 2 "один рубль и две копейки"

    [<Fact>]
    let ``1.05 → один рубль и пять копеек`` () =
        expectKopeckSpelling 1L 5 "один рубль и пять копеек"

    [<Fact>]
    let ``1.11 → один рубль и одиннадцать копеек`` () =
        expectKopeckSpelling 1L 11 "один рубль и одиннадцать копеек"

    [<Fact>]
    let ``1.21 → один рубль и двадцать одна копейка`` () =
        expectKopeckSpelling 1L 21 "один рубль и двадцать одна копейка"

    [<Fact>]
    let ``1.22 → один рубль и двадцать две копейки`` () =
        expectKopeckSpelling 1L 22 "один рубль и двадцать две копейки"

    [<Fact>]
    let ``1.25 → один рубль и двадцать пять копеек`` () =
        expectKopeckSpelling 1L 25 "один рубль и двадцать пять копеек"

    [<Fact>]
    let ``1.99 → один рубль и девяносто девять копеек`` () =
        expectKopeckSpelling 1L 99 "один рубль и девяносто девять копеек"

    /// === Проверка при 999 рублях (максимум) ===

    [<Fact>]
    let ``999.01 → девятьсот девяносто девять рублей и одна копейка`` () =
        expectKopeckSpelling 999L 1 "девятьсот девяносто девять рублей и одна копейка"

    [<Fact>]
    let ``999.02 → девятьсот девяносто девять рублей и две копейки`` () =
        expectKopeckSpelling 999L 2 "девятьсот девяносто девять рублей и две копейки"

    [<Fact>]
    let ``999.05 → девятьсот девяносто девять рублей и пять копеек`` () =
        expectKopeckSpelling 999L 5 "девятьсот девяносто девять рублей и пять копеек"

    [<Fact>]
    let ``999.11 → девятьсот девяносто девять рублей и одиннадцать копеек`` () =
        expectKopeckSpelling 999L 11 "девятьсот девяносто девять рублей и одиннадцать копеек"

    [<Fact>]
    let ``999.21 → девятьсот девяносто девять рублей и двадцать одна копейка`` () =
        expectKopeckSpelling 999L 21 "девятьсот девяносто девять рублей и двадцать одна копейка"

    [<Fact>]
    let ``999.22 → девятьсот девяносто девять рублей и двадцать две копейки`` () =
        expectKopeckSpelling 999L 22 "девятьсот девяносто девять рублей и двадцать две копейки"

    [<Fact>]
    let ``999.25 → девятьсот девяносто девять рублей и двадцать пять копеек`` () =
        expectKopeckSpelling 999L 25 "девятьсот девяносто девять рублей и двадцать пять копеек"

    [<Fact>]
    let ``999.99 → девятьсот девяносто девять рублей и девяносто девять копеек`` () =
        expectKopeckSpelling 999L 99 "девятьсот девяносто девять рублей и девяносто девять копеек"